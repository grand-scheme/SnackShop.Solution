@{
  Layout = "_Layout";
}

@using SnackShop.Models;
@model List<SnackShop.Models.Treat>;
  @if (@Model.Count == 0)
  {
    <p>
      NOTHING
    </p>
  }
  else
  {
    @foreach (Treat treat in Model) 
    {
      @Html.ActionLink($"{treat.TreatName}", "Read", new { id = treat.TreatId});
    }
  }

  @Html.ActionLink("Add", "Create")